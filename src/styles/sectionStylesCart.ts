import styled from 'styled-components';
const SectionStylesCart = styled.section`
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    align-items: center;
    width:1250px;
`;

export default SectionStylesCart;
